# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Signing keys
~/
.env

# Claude Code
.claude/
UPDATE.md
# AGENTS.md is NOT ignored. A bare `AGENTS.md` pattern matches at every depth,
# so it silently kept the per-directory rule files out of the repository while
# the tracked root file (added before the pattern) went on looking fine.

# Tauri build output
src-tauri/target/
src-tauri/gen/
src-tauri/resources/maintenance-helper.exe

# Unused template assets
public/tauri.svg
public/vite.svg
src/assets/react.svg

# Rust workspace
target/

# Bootstrapper
bootstrapper/assets/nsis-payload.exe
